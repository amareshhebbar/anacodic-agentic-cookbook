# Stage 01 — extract. Install from the repo root:
#   pip install -r requirements.txt                       # shared nbio plumbing
#   pip install -r 01-modules/01-tools/01-extract/requirements.txt   # this stage

# 01-pdf-printed, 02-tables-and-layout — the docling PDF pipeline
docling>=2.100
pymupdf>=1.24      # PDF metadata + page-to-image rendering (imported as `fitz`)

# All four notebooks — image handling, .env loading
pillow>=10.0
python-dotenv>=1.0

# 03-orientation — stands in for Kraken in this sandbox (see the notebook's
# first cell for why). Any OCR engine that returns per-line text + confidence
# would slot in the same way; this one needs no model download and no GPU.
rapidocr>=1.3

# 04-handwriting-ocr — the Groq vision HTTP call, and its YAML-driven
# post-OCR pitfall fixer
httpx>=0.27
pyyaml>=6.0

# --- Optional, not required to run any notebook here ---
# kraken            # real engine `03-orientation` ports (recognize_with_kraken)
#                   # but does not require: no model ships with this repo (see
#                   # the secrets/exclusions PRD), and ocr_engines.py's own
#                   # design already degrades to an empty result without it.
