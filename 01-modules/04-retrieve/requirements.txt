# 01-modules/04-retrieve — stage-specific dependencies.
# Install alongside the root requirements.txt:
#   pip install -r requirements.txt -r 01-modules/04-retrieve/requirements.txt

# Source clients in 02-multi-source-fanout.ipynb (pubmed/openalex/clinicaltrials/tavily/
# semantic-scholar) are ported using stdlib urllib, same as the donor code -- no HTTP
# client package is required for them to run.

groq>=1.0                # 04-llm-chunk-scoring.ipynb — live RCS scoring if GROQ_API_KEY is set
openai>=1.0               # 04-llm-chunk-scoring.ipynb — alternative provider if OPENAI_API_KEY is set instead
pinecone>=5.0             # 01-single-index-retrieval.ipynb — referenced production backend (course_rag.py's
                          # Pinecone path is shown but not exercised; the notebook runs on the local fallback)
rank-bm25>=0.2            # 02-multi-source-fanout.ipynb — standalone BM25 demo standing in for
                          # sparse_retrieval.py's pinecone-text hybrid encoder (which needs a fitted,
                          # unshipped pickle)
