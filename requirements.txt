# Root-level: shared notebook plumbing only.
# Each stage folder pins what that stage needs — 01-modules/*/requirements.txt,
# and 01-modules/01-tools/*/requirements.txt for the sub-stages inside tools.
# Install those too when working in a specific stage.
python-dotenv>=1.0
jupyter>=1.0
nbformat>=5.10
nbstripout>=0.7
pre-commit>=3.7
