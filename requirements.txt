# Root-level: shared notebook plumbing only.
# Each stage folder (01-modules/*/requirements.txt) pins what that stage needs —
# install those too when working in a specific stage.
python-dotenv>=1.0
jupyter>=1.0
nbformat>=5.10
nbstripout>=0.7
pre-commit>=3.7
